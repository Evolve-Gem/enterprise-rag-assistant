# Enterprise RAG Copilot — backend dependencies
#
# Install:  pip install -r backend/requirements.txt
#
# Version pins reflect the environment in which this project is verified.

# --- web framework -----------------------------------------------------------
fastapi==0.136.1
uvicorn[standard]==0.47.0
python-multipart==0.0.28
pydantic==2.13.4

# --- model + embeddings ------------------------------------------------------
openai==2.37.0
python-dotenv==1.2.2

# --- vector math -------------------------------------------------------------
numpy>=2.0,<3

# --- document extraction -----------------------------------------------------
pypdf==6.19.0
python-docx==1.2.0

# --- agent orchestration -----------------------------------------------------
# LangGraph powers the agent workflow.  The backend also ships a dependency-free
# native state machine, so AGENT_ENGINE=native works with this line removed.
langgraph==1.2.11

# --- tests -------------------------------------------------------------------
pytest==9.1.1
httpx==0.28.1

# --- optional: PostgreSQL + pgvector vector store ----------------------------
# Only needed when VECTOR_STORE=pgvector.  See docker-compose.yml (profile "pg").
# psycopg[binary]>=3.2
