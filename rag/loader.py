from pathlib import Path


def load_markdown_documents(knowledge_base_dir: str | Path) -> list[dict]:
    """Load Markdown documents from the knowledge base directory.

    The real document loading logic will be implemented in a later stage.
    """
    # TODO: Read Markdown files and return normalized document objects.
    return []
