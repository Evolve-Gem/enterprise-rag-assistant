# 2026-09-18 · Enterprise RAG Copilot V3 — Phase B 域名切换

## 结果

**READY FOR PUBLIC DEMO** ✅ —— `https://rag.changziqi.com` 已从 legacy V2 (8502) 安全切换至 V3 (3001)。

## 执行记录

1. **切换前复核（全只读）**：3001 V3 healthy（24/283/langgraph）、8502 legacy 200、18000 仅 127.0.0.1、
   80/443 nginx 正常、目标确认 = `sites-enabled/rag.changziqi.com` 第 6 行 `proxy_pass ... :8502;`。
2. **备份**：`/etc/nginx/sites-available/rag.changziqi.com.bak.20260918-102043`（1146 B，与原文件逐字节一致）。
3. **最小修改**：仅该文件一行 8502→3001（diff 对备份验证）；未触碰其他域名/证书/legacy。
4. **`nginx -t`** PASS（syntax ok + test successful）→ 才 `systemctl reload nginx`。
5. **HTTPS 验证**：域名 200、证书 `CN=rag.changziqi.com`（至 2026-12-09，未重签）、HTTP→HTTPS 301、
   `/health` 服务器侧+本机外网侧均返回 V3 payload、无 502/504。
6. **Golden Smoke（API 层，全部经域名）**：登录 401/200、Overview 24/283、Ask 422 字+3 引用、
   Citation chunk（doc/section/snippet/score）、Agent `kb_gap_analysis` 8 节点 2 skipped、
   Solution Studio 8 章节+4 引用、Gaps/Evaluation 200、只读上传 **403 read_only**、无 `sk-`/`Traceback` 泄漏。
7. **真实浏览器（CDP，脚本 `C:\agents\temp\phaseB_browser.mjs`）**：密码门渲染 ✓、错误密码拒绝 ✓、
   正确密码进入 ✓、Overview 含 283/langgraph ✓（截图 `phaseB_overview.png`）、
   6 路由（ask/agent/solution-studio/gaps/evaluation/documents）**console error=0 / page error=0**。
   教训：脚本内 `/\\b24\\b/` 双重转义导致该断言失效（检查代码 bug，非产品问题）；
   chunk_id 含中文必须 `urllib.parse.quote` 再拼 URL。
8. **重启恢复**：`docker compose restart` 后域名 `/health` 第 2 次轮询 200；Overview/Ask/Agent 复测正常。
9. **Legacy 保留**：`enterprise-rag-demo` Up 3 weeks healthy，8502=200，未删容器/镜像/端口。
10. **回滚路径**：`sudo cp .../rag.changziqi.com.bak.20260918-102043 .../rag.changziqi.com && sudo nginx -t && sudo systemctl reload nginx`。

## Git

- 报告更新提交 **`bb2f5fe`**（docs/DEPLOYMENT_REPORT.md：§14 + 头部/§9/§11/§12/§13.5 过时表述清理），
  已推送远端（`aac616b..bb2f5fe` MATCH）。
- 报告历史判定（"未部署/未公网验证"）已标注为历史记录，报告不再自相矛盾。

## 可复用

- 域名切换 SOP = 复核(只读) → 备份 → 单行 diff 验证 → `nginx -t` → reload → 双侧 HTTPS 验证 → 冒烟 → 重启复验 → 回滚确认。
- 浏览器冒烟脚本 `phaseB_browser.mjs`（CDP，密码走 env 不落盘）可复用于后续 UI 验收。
